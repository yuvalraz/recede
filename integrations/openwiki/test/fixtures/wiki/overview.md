# Overview

This project turns source text into a structured document. The pipeline reads
input, parses it, and emits output.

There are no direct source citations on this page — it is pure prose, which
exercises the conservative rule: a page that cites nothing decays on ANY code
diff, because we cannot prove the change is unrelated to it.
