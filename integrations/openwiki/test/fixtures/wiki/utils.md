# Utilities

Shared helpers live under the utils module. The key export is
[helperFn](src/utils.ts#helperFn), used across the parser and the emitter.

Refer to `src/utils.ts#helperFn` when debugging shared formatting.
